<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record model="ir.ui.view" id="view_pedido_abierto_wizard">
            <field name="model">pedido.abierto.wizard</field>
            <field name="arch" type="xml">
                <form string="Pedido abierto">
                    <field name="pedido_abierto_id" invisible="1"/>
                    <field name='lineas_pedidos' string=' ' attrs="{'domain': [('es_de_sale_order', '=', False)]}">
                        <tree class="slae_line_edit" editable="bottom">
                            <field name="sequence" widget="handle"/>
                            <!-- We do not display the type because we don't want the user to be bothered with that information if he has no section or note. -->
                            <field name="display_type" invisible="1"/>
                            <field name="product_uom_category_id" invisible="1"/>
                            <field name="product_updatable" invisible="1"/>
                            <field name="product_id"
                                   attrs="{                                             'readonly': [('product_updatable', '=', False)],                                             'required': [('display_type', '=', False)],                                         }"
                                   options="{'no_open': True}" force_save="1"
                                   widget="product_configurator" readonly="1"/>


                            <field name="pedido_abierto_rel" invisible="1"/>
                            <field name="default_code_product" readonly="1" optional="show"/>
                            <field name="codigo_cliente" readonly="1" optional="show"/>
                            <field name="pedido_cliente" optional="show" readonly="1"/>
                            <field name="codigo_alterno" optional="show" readonly="1"/>
                            <field name="fecha_programada" optional="show" readonly="1"/>
                            <field name="cantidad_reservada" force_save="1" optional="show" readonly="1"/>
                            <field name="order_partner_id" invisible="1"/>
                            <!--<field name="order_id" invisible="1"/>-->

                            <field name="name" widget="section_and_note_text" optional="show" readonly="1"/>

                            <field name="product_uom_qty"/>
                            <field name="cantidad_facturada" readonly="1"/>
                            <field name="cantidad_entregada" readonly="1"/>
                            <field name="cantidad_restante" readonly="1"/>
                            <field name="cantidad_original" readonly="1"/>


                            <field name="qty_delivered" string="Delivered" optional="show" readonly="1"/>
                            <field name="qty_delivered_manual" invisible="1"/>
                            <field name="qty_delivered_method" invisible="1"/>
                            <field name="qty_invoiced" string="Invoiced" optional="show" readonly="1"/>
                            <field name="qty_to_invoice" invisible="1"/>
                            <field name="product_uom" force_save="1" string="UoM" options="{&quot;no_open&quot;: True}" optional="show" readonly="1"/>
                            <field name="customer_lead" optional="hide" readonly="1"/>
                            <field name="price_unit" attrs="{'readonly': [('qty_invoiced', '&gt;', 0)]}" readonly="1"/>
                            <field name="tax_id" widget="many2many_tags" options="{'no_create': True}"
                                   attrs="{'readonly': [('qty_invoiced', '&gt;', 0)]}" optional="show" readonly="1"/>
                            <field name="discount" string="Disc.%" groups="product.group_discount_per_so_line"
                                   optional="show" readonly="1"/>
                            <field name="price_subtotal" widget="monetary"
                                   groups="account.group_show_line_subtotals_tax_excluded" readonly="1"/>
                            <field name="price_total" widget="monetary"
                                   groups="account.group_show_line_subtotals_tax_included" readonly="1"/>
                            <field name="state" invisible="1"/>
                            <field name="invoice_status" invisible="1"/>
                            <field name="currency_id" invisible="1"/>
                            <field name="price_tax" invisible="1"/>
                            <field name="company_id" invisible="1"/>


                        </tree>

                    </field>

                    <footer>
                        <button name="crear_orden" string="Crear orden" type="object" class="oe_highlight"/>
                        <button special="cancel" string="Cerrar" class="oe_highlight"/>
                    </footer>

                </form>
            </field>
        </record>


    </data>
</odoo>