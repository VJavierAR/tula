<odoo>
    <data>
        <record model="ir.ui.view" id="stock.vpicktree">
            <field name="name">stock.picking.tree</field>
            <field name="model">stock.picking</field>
            <field name="arch" type="xml">
                <tree string="Picking list"
                      multi_edit="1"
                      decoration-info="state == 'draft'"
                      decoration-muted="state == 'cancel'"
                      decoration-danger="state == 'confirmed'"
                      decoration-warning="state == 'waiting'"
                      decoration-succes="state == 'done'"

                    >
                    <!-- estados: draft, waiting, confirmed, assigned, done, cancel
                    decoration-danger="state not in ('cancel', 'done') and scheduled_date &lt; current_date"
                    -->
                    <field name="name"/>
                    <field name="location_id" options="{'no_create': True}" string="From" groups="stock.group_stock_multi_locations" optional="show"/>
                    <field name="location_dest_id" options="{'no_create': True}" string="To" groups="stock.group_stock_multi_locations" optional="show"/>
                    <field name="partner_id" optional="show"/>
                    <field name="user_id" optional="hide"/>
                    <field name="date" invisible="1"/>
                    <field name="scheduled_date" optional="show"/>
                    <field name="origin" optional="show"/>
                    <field name="group_id" invisible="1"/>
                    <field name="backorder_id" optional="hide"/>
                    <field name="state" optional="show"/>
                    <field name="priority" optional="hide"/>
                    <field name="picking_type_id" optional="hide"/>
                    <field name="company_id" groups="base.group_multi_company" optional="show"/>
                    <field name="activity_exception_decoration" widget="activity_exception"/>
                </tree>
            </field>
        </record>

        <record model="ir.ui.view" id="stock_picking_form_update">
            <field name="name">stock.picking.form.update</field>
            <field name="model">stock.picking</field>
            <field name="inherit_id" ref="stock.view_picking_form"/>
            <field name="arch" type="xml">
                <xpath expr="//button[@name='action_cancel']" position="after">
                    <button name="print_vale_de_entrega" type="object" string="Imprimir vale de entrega" class="oe_highlight"/>
                </xpath>
            </field>
        </record>

    </data>
</odoo>